<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Movie List</title>
</head>
<body>
  <h1>Movie List on selected theater</h1>
  <ul>
    <% movies.forEach(function(movie) { %>
      <li>
        <!-- detail -->
          <a href="/detail/<%= movie.location %>/<%= movie.movie_id %>"><%= movie.title  %></a>
      </li>
    <% }) %>
  </ul>
</body>
</html>
