<%-include("./partials/header")-%>
<!DOCTYPE html>
<html>
<head>
    <title>Register</title>
</head>
    <body>
        <% if (UsernameAvailability == false) { %>
            <p>Username is already taken.</p>
        <% } %>
        <% if (PhoneNumberAvailability == false) { %>
            <p>Phone number is already taken.</p>
        <% } %>
        <% if ( EmailAvailability == false) { %>
            <p>Email is already taken.</p>
        <% } %>
        
        <% if (usernameRegexAllowed == false) { %>
            <p>consist of 8 to 30 characters, including uppercase and lowercase letters, numbers, underscores, and hyphens..</p>
        <% } %>
        <% if (emailRegexAllowed == false) { %>
            <p>Ensures that the email address contains a username part, followed by the "@" symbol, a domain part, and a valid top-level domain.</p>
        <% } %>
        <% if (passwordRegexAllowed == false) { %>
            <p>//At least one lowercase letter,  at least one uppercase letter, at least one digit, matches any combination of alphanumeric characters and special characters (non-whitespace) with a minimum length of 8..</p>
        <% } %>
        <% if (phoneNumberRegexAllowed == false) { %>
            <p>presence of a country code, parentheses around the area code, and the use of hyphens, dots, or spaces as separators.</p>
        <% } %>
        <div id="registerModal" class="fixed inset-0 flex items-center justify-center z-50">
            <div class="bg-white rounded-lg shadow-lg p-6 w-1/2">
                <h2 class="text-2xl font-semibold mb-4">Register</h2>
    
        <form method="POST" action="/register">
            <div class="mb-4">
                <label for="username" class="block text-gray-700 font-semibold mb-2">Username</label>
                <input type="text" id="username" name="username"
                    class="border border-gray-300 rounded px-4 py-2 w-full" required>
            </div>

            <br><br>

            <div class="mb-4">
                <label for="email" class="block text-gray-700 font-semibold mb-2">Email</label>
                <input type="email" id="email" name="email" class="border border-gray-300 rounded px-4 py-2 w-full"
                    required>
            </div>

            <br><br>

            <div class="mb-4">
                <label for="password" class="block text-gray-700 font-semibold mb-2">Password</label>
                <input type="password" id="password" name="password"
                    class="border border-gray-300 rounded px-4 py-2 w-full" required>
            </div>

            <br><br>

            <div class="mb-4">
                <label for="password" class="block text-gray-700 font-semibold mb-2">Confirm Password</label>
                <input type="password" id="confirmPassword" name="confirmPassword"
                    class="border border-gray-300 rounded px-4 py-2 w-full" required>
            </div>

            <br><br>

            <div class="mb-4">
                <label for="firstName" class="block text-gray-700 font-semibold mb-2">First Name</label>
                <input type="text" id="firstName" name="firstName"
                    class="border border-gray-300 rounded px-4 py-2 w-full" required>
            </div>

            <br><br>

            <div class="mb-4">
                <label for="lastName" class="block text-gray-700 font-semibold mb-2">Last Name</label>
                <input type="text" id="lastName" name="lastName"
                    class="border border-gray-300 rounded px-4 py-2 w-full" required>
            </div>

            <br><br>

        <div class="mb-6">
            <label for="phoneNumber" class="block text-gray-700 font-semibold mb-2">Phone Number</label>
            <input type="tel" id="phoneNumber" name="phoneNumber"
                class="border border-gray-300 rounded px-4 py-2 w-full" required>
        </div>

            <br><br>
            <div class="text-right">
                <button type="submit"
                    class="bg-red-600 hover:bg-red-700 text-white font-semibold py-2 px-4 rounded"onclick="return comparePasswords()">Register</button>
            </div>
        </form>
        <path fill="none" d="M0 0h24v24H0z" />
        <path
            d="M19 6.41l-1.41-1.41L12 10.17 6.41 4.59 5 6l6 6-6 6 1.41 1.41L12 13.83l5.59 5.59 1.41-1.41L13.83 12l5.59-5.59z" />
        </svg>
        <button id="closeModalBtn" class="absolute top-0 right-0 mt-4 mr-4 text-white hover:text-gray-800">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" class="h-6 w-6 close-icon">
                <path fill="none" d="M0 0h24v24H0z" />
                <path
                    d="M19 6.41l-1.41-1.41L12 10.17 6.41 4.59 5 6l6 6-6 6 1.41 1.41L12 13.83l5.59 5.59 1.41-1.41L13.83 12l5.59-5.59z" />
            </svg>
        </button>
        <script>
            function comparePasswords() {
                var password = document.getElementById("password").value;
                var confirmPassword = document.getElementById("confirmPassword").value;
    
                if (password !== confirmPassword) {
                    alert("Passwords do not match.");
                    return false; // Prevent form submission
                }
                
                return true; // Allow form submission
            }
        </script>
        </div>
        </div>
    </body>
</html>


   